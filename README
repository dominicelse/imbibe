To install on Linux (probably works on Mac too):

Install python3
Run:
> pip3 install arxiv habanero
copy autobib/ to somewhere in $PYTHONPATH
copy autobib/autobib to somewhere in $PATH and make it executable
